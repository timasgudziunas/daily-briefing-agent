"""Price / market-data lookups (Phase 2).

Outcome data for grading: `yfinance` for prices (primary) with `finnhub-python`
as fallback, plus FRED for data outcomes. Used to anchor grades to real numbers.

TODO (Phase 2): implement `get_price(ticker, date)` with yfinance->Finnhub fallback.
"""
