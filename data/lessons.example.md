# Lessons

Distilled, generalizable rules learned from graded predictions. Kept **small and
high-signal** — this file is fed into every AM run before it predicts, so noise
here costs tokens and dilutes judgment. The PM run appends a lesson only when a
graded miss reveals a rule worth keeping; curate aggressively.

_No lessons yet — the ledger is empty. The first lessons appear after the PM run
grades its first predictions._
